"use client";

import { Card, CardContent } from '@/components/ui/card';

export default function TestCard() {
  return (
    <Card>
      <CardContent>
        <p>Test Card</p>
      </CardContent>
    </Card>
  );
}